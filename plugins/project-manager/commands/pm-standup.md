---
description: Weekly standup digest across JFB repos - merged, open, blocked. Pass a repo name to focus.
argument-hint: "[repo-name]"
---

Run the **standup** capability from the `project-manager` skill.

If `$ARGUMENTS` is non-empty, narrow to that repo (partial match OK). Otherwise cover all JFB repos.
