---
description: Pitch 3-5 new feature/extension proposals across JFB repos, ranked with rationale.
argument-hint: "[repo-name]"
---

Run the **pitch** capability from the `project-manager` skill.

If `$ARGUMENTS` is non-empty, narrow proposals to that repo (partial match OK). Otherwise pitch across all JFB repos.
